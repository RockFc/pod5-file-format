# 压缩率对比 · 自有数据 · POD5 vs CCF5

> 系列文档命名：`压缩率对比_<数据来源>_POD5-vs-CCF5.md`  
> 姊妹篇（墨尔本 Figshare 公开 POD5）：[`压缩率对比_公开数据_POD5-vs-CCF5.md`](./压缩率对比_公开数据_POD5-vs-CCF5.md)

**结论先行：** 同一批自有数据（flow cell `260F401529011`，10 个文件，约 **15.1 GB** 裸 `int16`）：先从已有 `.ccf5` 导出 int16 算 CCF5 压缩率，再把同批 int16 用 `pod5-file-format`（`[mytest3]`，VBZ）写入 `.pod5` 算 POD5 压缩率：

| 口径 | 压缩倍数（原始 int16 / 压缩文件） | 体积节省 | 备注 |
|------|----------------------------------|----------|------|
| CCF5 | **1.476×** | **32.3%** | 实验 1：`.ccf5` → 导出 int16 |
| ONT POD5 | **1.545×** | **35.3%** | 实验 2：同批 int16 → `.pod5` |
| 两种格式体积差 | CCF5 大约大 **4.65%** | — | 约多 **456 MB** |

与公开数据篇（约 **2.35×**、两种格式差约 **0.3%**）对照：自有这批信号的**绝对压缩水平明显更低**（大约少压掉一整档），且两种格式差距也拉大到约 **4.7%**（POD5 更小）。说明「换实现」在这批数据上仍不是数量级差距，但**数据本身可压性**与公开 Figshare 两套差一截。

测试日期：2026-09-08。数据来源：自有批次 `260F401529011`（`260F401529011_1_1_0`～`_9`）。裸 `int16` 合计 **15.139 GB**（15 139 105 594 字节）；reads **86 779**；samples **7 569 552 797**。

---

## 1. 要回答什么问题

公开数据篇已经说明：在 Figshare 两套官方 `.pod5` 上，POD5 与 CCF5 整文件倍数差约 **0.3%**，绝对水平约 **2.3～2.4×**。还需要确认：

1. **换自有 ADC 数据**，先从 CCF5 导出 int16，再装进 POD5，同批波形下两种格式压缩倍数是否仍几乎一样？
2. 自有数据的绝对压缩水平是否仍落在 **2.3～2.4×** 一带，还是因采样率、噪声、read 长度等偏离公开数据？
3. 大批量（多文件）时，差异是否稳定，有没有个别文件异常？

**本次回答：**

1. 两种格式仍在同一量级，但差距约 **4.65%**（大于公开数据的 0.3%）；POD5 略优。
2. 绝对倍数约 **1.48～1.55×**，明显低于公开数据的 **2.3～2.4×**——主因更像是**这批信号本身更不好压**，而不是某一种格式「没压够」。
3. 10 个文件分布极窄（CCF5 1.473～1.479；POD5 1.543～1.547），没有个别异常文件。

---

## 2. 怎么测

**流程：** 先从已有 `.ccf5` 导出裸小端 `int16`（`.dat`；每点 2 字节）并算 CCF5 压缩率；再用同批导出的 `.dat` 写入 POD5 并算 POD5 压缩率。  
**禁止混批：** 每个对比单元是同一份波形字节：CCF5 体积取源 `.ccf5`，POD5 体积取由同一导出 `.dat` 新写入的 `.pod5`。

| 步骤 | 做什么 | 口径 |
|------|--------|------|
| 实验 1（CCF5） | 已有多 read `.ccf5`；`ut_ccf5_compression --extract` 导出同名 `.dat`，并汇总 int16 / `.ccf5` 体积 | 倍数 = `int16 字节 / .ccf5 字节` |
| 实验 2（POD5） | 以实验 1 导出的 `.dat` 为输入；`pod5-file-format` 的 `[mytest3]`：每个 `.dat` 整文件当一条 read，预压缩 VBZ 写入同名 `.pod5` | 倍数 = `int16 字节 / .pod5 字节` |
| 汇总 | 按文件 / 合计填 §3、§5 | 整文件（信号 + 表 + 容器） |

复现入口（顺序固定：先 CCF5 导出，再 POD5 写入）：

```bash
# 实验 1 · CCF5：已有 .ccf5 → 导出 int16，并得到 CCF5 压缩率（cytools）
#   输入: cytools/test_data/260F401529011_signals/*.ccf5
#   输出: cytools/test_data/int16_export_260F401529011_signals/*.dat
#   汇总: .../ccf5_extract_summary.md
ut_ccf5_compression --extract   # 或 cytools 内等价入口

# 实验 2 · POD5：实验 1 导出的同批 int16 → .pod5（pod5-file-format）
cd pod5-file-format && ./quick_test_mytest.sh "[mytest3]"
# 输入: 同上 int16_export_260F401529011_signals/*.dat
# 输出: /tmp/pod5_from_dat_int16_export_260F401529011_signals/
# 汇总: .../summary.tsv
```

口径说明：与公开数据篇一致，报**整文件**倍数。POD5 侧 `vbz_bytes` 与 `pod5_bytes` 几乎相等（容器开销可忽略）；CCF5 侧为多 read 文件，表/容器开销略大，但信号仍占绝对大头。  
另：实验 2 中 `pod5-file-format` 的 `[mytest3]` 把每个 `.dat` 写成**一条** read，与实验 1 源 `.ccf5` 的多 read 切分不同，主要影响元数据行数，不改变波形字节本身。

按 samples / reads ≈ **8.72×10⁴** 点/read：若按 **4000 Hz** 约 **21.8 s/read**，若按 **5000 Hz** 约 **17.4 s/read**（该批未在汇总里写死采样率，仅作量级参考）。

---

## 3. 结果

### 3.1 总表

体积用 \(10^9\) 字节记 GB；倍数按字节精确计算。

| 批次 / 数据集 | 文件数 | Reads | 裸 int16 | CCF5 | POD5 | CCF5 倍数 | POD5 倍数 | 体积差（CCF5 vs POD5） |
|---------------|-------:|------:|---------:|-----:|-----:|----------:|----------:|------------------------:|
| `260F401529011` | 10 | 86 779 | 15.139 GB | 10.255 GB | 9.800 GB | **1.476×** | **1.545×** | CCF5 大约大 **4.65%** |
| **合计** | **10** | **86 779** | **15.139 GB** | **10.255 GB** | **9.800 GB** | **1.476×** | **1.545×** | 约多 **456 MB** |

一句话结论：约 **15.1 GB** 原始波形（实验 1 从 CCF5 导出），CCF5 约 **10.3 GB**，同批 int16 写入 POD5 约 **9.8 GB**；两种格式差约 **4.7%**，绝对压缩水平明显低于公开 Figshare 两套（约 2.35×）。

### 3.2 与公开数据对照

| 对比 | 公开 Figshare（已测） | 自有数据（本次） | 含义 |
|------|----------------------|------------------|------|
| CCF5 整文件倍数 | **2.35×** | **1.476×** | 自有这批可压性明显更差 |
| POD5 整文件倍数 | **2.36×** | **1.545×** | 同上 |
| 两种格式体积差 | 约 **0.3%** | 约 **4.65%** | 仍非数量级，但已可感知 |
| 换批次后倍数是否仍稳 | AMtb vs Klebsiella 差约 1.3% | 该批 10 文件极稳；与公开数据差约 **35% 相对** | **数据批次**主导绝对水平 |

### 3.3 单文件分布（摘要）

| 指标 | CCF5 倍数 | POD5 倍数 |
|------|-----------|-----------|
| min / 中位 / max | **1.473 / 1.477 / 1.479** | **1.543 / 1.545 / 1.547** |
| 备注 | 10 文件全部落在 1.47× 窄带 | 10 文件全部落在 1.54× 窄带；逐文件 CCF5 均大约 4.6% |

不是平均值凑巧接近，而是每个文件都齐。

---

## 4. 建议

1. **不要拿公开数据的 2.3～2.4× 当自有盘占用预算。** 该批自有信号大约只有 **1.5×** 出头；估算存储请用自有实测，或先对代表性批次做同样的「CCF5 导出 int16 → 再写 POD5」对照。
2. **两种格式仍在同一水平。** CCF5 大约大 4.7%，值得记一笔，但远谈不上「官方能压、CCF5 压不动」。若要对齐 POD5 体积，应查 CCF5 写路径（chunk/read 切分、zlib 档位、表开销），而不是先换算法家族。
3. **优先查「为什么这批信号更不好压」。** 公开两套（不同菌种、read 长短差一倍）仍稳在 2.3×+；该批却掉到 1.5×。更可能是采样条件、噪声、预处理或导出切分导致相邻点可预测性变差——这和公开篇「数据稳定性定上限」的结论一致，只是该批落在更差的一侧。

---

## 5. 附录：测试数据（压缩率）

倍数 = 原始 int16 字节 / 压缩文件字节。体积单位 MB（\(10^6\) 字节）。每个文件一行，先 CCF5（实验 1）再 POD5（实验 2）。

### 5.1 数据清单

| 批次 ID | 路径 / 说明 | 采样率 | 备注 |
|---------|-------------|-------:|------|
| `260F401529011` | 实验 1：`cytools/test_data/260F401529011_signals/*.ccf5` → 导出 `int16_export_260F401529011_signals/*.dat`；实验 2：同批 `.dat` → `/tmp/pod5_from_dat_int16_export_260F401529011_signals/` | 未在汇总中写死（量级见 §2） | 10 文件；reads 86 779 |

### 5.2 逐文件结果

| 文件 | reads | int16 MB | CCF5 MB | CCF5 倍数 | POD5 MB | POD5 倍数 |
|------|------:|---------:|--------:|----------:|--------:|----------:|
| `260F401529011_1_1_0` | 9434 | 1473.13 | 1000.18 | 1.473 | 954.83 | 1.543 |
| `260F401529011_1_1_1` | 8768 | 1507.15 | 1020.76 | 1.476 | 975.40 | 1.545 |
| `260F401529011_1_1_2` | 8429 | 1515.68 | 1025.97 | 1.477 | 980.77 | 1.545 |
| `260F401529011_1_1_3` | 8795 | 1531.17 | 1035.23 | 1.479 | 989.75 | 1.547 |
| `260F401529011_1_1_4` | 8550 | 1506.64 | 1022.03 | 1.474 | 976.45 | 1.543 |
| `260F401529011_1_1_5` | 8521 | 1517.05 | 1028.39 | 1.475 | 982.69 | 1.544 |
| `260F401529011_1_1_6` | 8372 | 1518.39 | 1028.26 | 1.477 | 982.72 | 1.545 |
| `260F401529011_1_1_7` | 8922 | 1528.78 | 1034.89 | 1.477 | 988.83 | 1.546 |
| `260F401529011_1_1_8` | 8583 | 1523.32 | 1031.49 | 1.477 | 985.64 | 1.546 |
| `260F401529011_1_1_9` | 8405 | 1517.79 | 1028.00 | 1.476 | 982.49 | 1.545 |
| **合计** | **86 779** | **15139.11** | **10255.20** | **1.476** | **9799.57** | **1.545** |

### 5.3 原始汇总表路径

| 项 | 路径 |
|----|------|
| CCF5 源目录（实验 1 输入） | `/home/fuchao/workspace/code/cytools/test_data/260F401529011_signals/` |
| int16 导出目录（实验 1 输出 / 实验 2 输入） | `/home/fuchao/workspace/code/cytools/test_data/int16_export_260F401529011_signals/` |
| CCF5 summary | `/home/fuchao/workspace/code/cytools/test_data/int16_export_260F401529011_signals/ccf5_extract_summary.md` |
| POD5 输出目录（实验 2） | `/tmp/pod5_from_dat_int16_export_260F401529011_signals/` |
| POD5 summary | `/tmp/pod5_from_dat_int16_export_260F401529011_signals/summary.tsv` |

---

## 6. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-09-08 | 建档；命名与公开数据篇对齐为 `压缩率对比_<来源>_POD5-vs-CCF5.md` |
| 2026-09-08 | 填入首批自有数据 `260F401529011`：POD5 **1.545×**、CCF5 **1.476×**，CCF5 大约大 **4.65%**；绝对水平低于公开数据约 2.35× |
| 2026-09-09 | 调整实验步骤顺序：实验 1 先从 CCF5 导出 int16 算压缩率；实验 2 再将同批 int16 写入 POD5 比较；结论表 / §2 / §3 / §5 列序与路径说明同步 |
| 2026-09-09 | 对外汇报口径：去掉「本仓库 / 本项目」等指称，POD5 侧统一写明 `pod5-file-format` |
| 2026-09-09 | `[mytest3]` 输出改到 `/tmp/pod5_from_dat*`（不再写 `test_data/`） |
